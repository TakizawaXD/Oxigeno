export async function seedOrganizationData(orgId: string) {
  console.log('Seed de datos omitido para la organización:', orgId);
  // Se deja vacío para que la aplicación comience limpia de datos, lista para el cliente.
}
